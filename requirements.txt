# Tier-2 Fact-Check LLM — Python dependencies
# Pin minor versions for reproducibility; update via `pip-compile` in CI.

# Web framework
fastapi==0.111.1
uvicorn[standard]==0.30.1

# Task queue
rq==1.16.2
redis==5.0.7

# Database
psycopg2-binary==2.9.9

# Data validation
pydantic==2.7.4
pydantic-settings==2.3.4

# HTTP client (for callback webhooks — Checkpoint B+)
httpx==0.27.0

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.0          # also used as async test client
fakeredis==2.23.2
